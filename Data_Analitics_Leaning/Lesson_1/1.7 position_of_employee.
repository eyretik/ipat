worker = ['Olya', 'Silyutina', 350000, 4]

if worker[3] < 2:
    position = 'junior'
elif worker[3] == 2 or worker[3] <= 5:
    position = 'middle'
else:
    position = 'senior'

status = worker[0] + ' ' + worker [1] + ' is ' + position

print(status)
